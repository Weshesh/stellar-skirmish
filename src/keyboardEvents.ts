import * as PIXI from 'pixi.js'
import { publicVariables } from './publicVariables'

export namespace keyboardEvents {
    export class keyPressEvent({charCode, key}: KeyboardEvent) {
            if (charCode >= 49 && charCode <= 54 || charCode == 32) {
                console.log("lol")
                if (charCode === 32) {
                    if (publicVariables.Variables.getActiveColor() == 0xf28590) {
                        colorKey = 0x85c7f2;
                        return;
                    }
    let kana = new publicVariables.Variables.
                    publicVariables.);
                    return;
                }
    
            }
        }
    }